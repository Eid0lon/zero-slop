interface:
  display_name: "Perfect Design"
  short_description: "Premium product-specific UI design engineer"
  default_prompt: "Use $perfect-design --contract before UI work, then build, no-slop gate, judge, and verify."
policy:
  allow_implicit_invocation: true
